import { LoadingDirective } from './loading.directive';

describe('LoadingDirective', () => {
	it('should create an instance', () => {
		const directive = new LoadingDirective(undefined, undefined, undefined);
		expect(directive).toBeTruthy();
	});
});
